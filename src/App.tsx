import React, { useState, useEffect, useRef } from "react";
import Layout from "./components/Layout";

const App = () => {
  return (
    <main>
      <Layout />
    </main>
  );
};

export default App;
