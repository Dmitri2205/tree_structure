import styled,{css} from "styled-components";

export const AsideContainer = styled.div`
    display:block;
    padding:0;
    margin:0;
    && > div.offcanvas {
        border: 3px solid cyan;
    }
`;